{<% var ngVer = "1.3.0" %>
  "name": "<%= _.slugify(_.humanize(appname)) %>",
  "version": "0.0.0",
  "dependencies": {
    "ox-core": "git@server.hayloft-it.ch:ox/ox-core-ui.git#upgrade"
  },
  "devDependencies": {
    "angular-mocks": "^<%= ngVer %>",
    "angular-scenario": "^<%= ngVer %>"
  }<% if (appPath) { %>,
  "appPath": "<%= appPath %>"<% } %>
}
