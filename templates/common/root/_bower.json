{<% var ngVer = "1.3.0" %>
  "name": "<%= _.slugify(_.humanize(appname)) %>",
  "version": "1.0.0",
  "homepage": "ox.hayloft-it.ch/ui/<%= _.slugify(_.humanize(appname)) %>",
  "description": "",
  "private": true,
  "ignore": [
    "node_modules",
    "bower_components"
  ],
  "dependencies": {
    "ox-core": "git@gitlab.hayloft-it.ch:office-x-ui/ox-core.git"
  },
  "devDependencies": {
    "angular-mocks": "^<%= ngVer %>",
    "angular-scenario": "^<%= ngVer %>"
  }<% if (appPath) { %>,
  "appPath": "src"<% } %>
}
